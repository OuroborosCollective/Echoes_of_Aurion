# Loaded in nginx http context before the arelogic.space server block.
limit_req_zone $binary_remote_addr zone=aurion_zone_upgrade:10m rate=10r/m;
limit_conn_zone $binary_remote_addr zone=aurion_zone_connections:10m;
