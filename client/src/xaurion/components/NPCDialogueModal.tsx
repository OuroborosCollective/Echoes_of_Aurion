import { deterministicIndex } from "@shared/deterministicSimulation";
import React,{useMemo}from'react';import type{NPCCharacter,Quest,RPGItem}from'../types';import type{GenkitAdapter}from'../adapters/GenkitAdapter';export const NPCDialogueModal:React.FC<{isOpen:boolean;onClose:()=>void;npc:NPCCharacter|null;activeQuests:Quest[];playerGold:number;playerLevel:number;genkitAdapter?:GenkitAdapter;onAcceptQuest:(q:Quest)=>void;onBuyItem:(i:RPGItem)=>void}>=p=>{const line=useMemo(()=>p.npc?.dialogue[deterministicIndex(`npc-dialogue-v1:${p.npc.id}`,Math.max(1,p.npc.dialogue.length))]??'',[p.npc,p.isOpen]);if(!p.isOpen||!p.npc)return null;return <div className="fixed inset-0 z-[130] grid place-items-center bg-black/80 p-4"><section className="w-full max-w-2xl rounded-2xl border border-cyan-400/30 bg-[#0d1519] p-5 text-white"><header className="flex justify-between"><div><b className="font-serif text-xl">{p.npc.name}</b><small className="block text-cyan-300">{p.npc.title}</small></div><button onClick={p.onClose}>✕</button></header><blockquote className="my-5 border-l-2 border-amber-300 pl-4 text-slate-200">“{line}”</blockquote>{p.npc.quests.length>0&&<div className="space-y-2">{p.npc.quests.map(q=><button key={q.id} onClick={()=>p.onAcceptQuest(q)} className="block w-full rounded-xl border border-amber-500/30 p-3 text-left"><b>{q.title}</b><small className="block text-slate-400">{q.objective}</small></button>)}</div>}<div className="mt-4 grid grid-cols-2 gap-2">{p.npc.shopItems?.slice(0,6).map(i=><button key={i.id} disabled={p.playerGold<i.valueGold} onClick={()=>p.onBuyItem(i)} className="rounded-xl border border-slate-700 p-2 text-left disabled:opacity-40"><span>{i.icon}</span> {i.name}<small className="block text-amber-300">🪙 {i.valueGold}</small></button>)}</div></section></div>};
