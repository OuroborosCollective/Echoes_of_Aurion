import type{CharacterClassId,PartyMember,Quest,SimulatedPlayer,WorldMobEntity}from'../types';import{MMORPG_CLASSES}from'../data/mmorpgData';export class PartyManager{public members:PartyMember[]=[];public maxPartySize=5;public onPartyMessage?:(sender:string,text:string)=>void;public onPartyQuestProgress?:(questId:string,objective:string,memberName:string)=>void;constructor(playerName='Explorer',playerClass:CharacterClassId='knight',playerLevel=1){this.createSoloParty(playerName,playerClass,playerLevel)}public createSoloParty(name:string,c:CharacterClassId,l:number){const d=MMORPG_CLASSES[c];this.members=[{id:'local_player',name,classId:c,className:d.name,level:l,hp:d.baseHp,maxHp:d.baseHp,resource:d.baseResource,maxResource:d.baseResource,resourceName:d.resourceName,resourceColor:d.resourceColor,isLeader:true,avatarIcon:d.icon,zone:'Aurion',isOnline:true,dps:0}]}public updatePlayerStats(hp:number,maxHp:number,resource:number,maxResource:number,level:number,zone:string){const m=this.members[0];if(m)Object.assign(m,{hp,maxHp,resource,maxResource,level,zone})}public getMembers(){return this.members.map(m=>({...m}))}public getOtherMembers(){return this.members.slice(1)}public isInParty(){return this.members.length>1}public inviteMember(p:SimulatedPlayer){if(this.members.length>=this.maxPartySize||this.members.some(m=>m.id===p.id))return false;const d=MMORPG_CLASSES[p.classId];this.members.push({id:p.id,name:p.name,classId:p.classId,className:d.name,level:p.level,hp:d.baseHp,maxHp:d.baseHp,resource:d.baseResource,maxResource:d.baseResource,resourceName:d.resourceName,resourceColor:d.resourceColor,isLeader:false,avatarIcon:d.icon,zone:'Aurion Open World',isOnline:true,dps:10+p.level*3});this.onPartyMessage?.('System',`${p.name} joined the party.`);return true}public removeMember(id:string){const i=this.members.findIndex(m=>m.id===id&&!m.isLeader);if(i<0)return false;this.members.splice(i,1);return true}public leaveParty(){this.members=this.members.slice(0,1)}public promoteToLeader(id:string){if(!this.members.some(m=>m.id===id))return false;this.members.forEach(m=>m.isLeader=m.id===id);return true}public update(_delta:number,_combat:boolean,_mob:WorldMobEntity|null){}public handleSharedKill(_mobName:string,quests:Quest[]){const q=quests.find(x=>!x.completed);return{sharedCount:Math.max(0,this.members.length-1),questTitle:q?.title}}}
