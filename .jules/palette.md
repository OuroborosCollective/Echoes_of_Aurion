# UX Learning: Disabled States\n\n- Added `disabled:cursor-not-allowed` (Tailwind) to disabled buttons (like in `Home.tsx` and `Community.tsx`) to provide better visual affordance that an action is currently unavailable.\n- Added `title` attributes on community buttons when they are disabled to give screen readers and hover states contextual explanations (e.g. 'Nur für angemeldete Explorer verfügbar').\n- Kept changes small and utilized existing Tailwind CSS classes, without adding new dependencies or custom CSS.

## 2024-09-11 - [Aurion Portal Interactive Elements Tactile Feedback]
**Learning:** [Applied `group`, `hover:-translate-y-0.5`, `active:scale-95`, and `transition-all` classes to create tactile feedback on the grid buttons, and `transition-transform group-hover:scale-110` for smooth hover scaling of internal Lucide icons. This aligns with standard UX practices in the application, ensuring interactive elements feel responsive and cohesive without needing custom CSS.]
**Action:** [Consistently reuse this specific combination of utility classes (`group hover:-translate-y-0.5 active:scale-95 transition-all`) when enhancing other card-like or block interactive grid elements across Aurion Portal for consistent feedback.]
