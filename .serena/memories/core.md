# Aurion project core
- Runtime target: Echoes_of_Aurion; Wasd/Areloria is semantic source only. No blind runtime copy, MSW/Lua, or mandatory 10-Hz tick.
- World invariants: deterministic seed + integer chunks + stable ordering; server-authoritative mutations; immutable delta/epoch receipts; private Tower home remains safe.
- Current audio candidate branch: `feature/aurion-audio-system`, based on main `73542eabfe981135593676648d5a2717f3b2c0a8`.
- Key focused memories: `mem:tech_stack` for tools/dependencies; `mem:suggested_commands` for commands; `mem:conventions` for code patterns; `mem:task_completion` for completion gates.