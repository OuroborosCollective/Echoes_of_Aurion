name: Apply clean companion learning evidence fix

on:
  push:
    branches:
      - fix/companion-learning-evidence-clean-20260828

permissions:
  contents: write

concurrency:
  group: apply-companion-learning-evidence-clean
  cancel-in-progress: false

jobs:
  patch-and-verify:
    runs-on: ubuntu-24.04
    timeout-minutes: 25

    steps:
      - name: Guard repository and branch
        run: |
          test "${GITHUB_REPOSITORY}" = "OuroborosCollective/Echoes_of_Aurion"
          test "${GITHUB_REF_NAME}" = "fix/companion-learning-evidence-clean-20260828"

      - name: Check out exact branch head
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          ref: fix/companion-learning-evidence-clean-20260828

      - name: Set up pnpm
        uses: pnpm/action-setup@v4

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install locked dependencies
        run: pnpm install --frozen-lockfile

      - name: Apply bounded source patch
        run: python3 scripts/apply_companion_learning_evidence_clean.py

      - name: Verify companion contracts
        run: |
          set -euo pipefail
          pnpm check
          pnpm vitest run \
            client/src/lib/companionLearning.test.ts \
            client/src/lib/companionFrameCapture.test.ts \
            server/companionMemory.test.ts
          git diff --check

      - name: Guard the review surface
        run: |
          set -euo pipefail
          changed="$(git status --short | awk '{print $2}' | sort)"
          expected="$(printf '%s\n' \
            client/src/components/GameCanvas.tsx \
            client/src/lib/companionFrameCapture.test.ts \
            client/src/lib/companionFrameCapture.ts \
            client/src/lib/companionLearning.test.ts \
            client/src/lib/companionLearning.ts \
            client/src/pages/Home.tsx | sort)"
          test "$changed" = "$expected"

      - name: Commit verified fix to the same branch
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add -A
          git commit -m "fix(companion): bind learning samples to real frames"
          git push origin HEAD:fix/companion-learning-evidence-clean-20260828
